import { ZippyContentDirectiveDirective } from './zippy-content-directive.directive';

describe('ZippyContentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZippyContentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
